[project]
name = "dbt-portfolio"
version = "0.1.0"
description = "stackoverflow yearly inflation adjustment"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-duckdb>=1.1.1",
]
